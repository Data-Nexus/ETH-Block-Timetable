{
  "name": "eth-block-timetable",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ eth-block-timetable",
    "create-local": "graph create --node http://localhost:8020/ eth-block-timetable",
    "remove-local": "graph remove --node http://localhost:8020/ eth-block-timetable",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 eth-block-timetable"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
